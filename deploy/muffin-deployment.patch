diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 1cbc379..b6eb3ba 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -11,7 +11,8 @@ name: Deploy to Oracle Cloud
 #   OPENROUTER_API_KEY, LANGSMITH_API_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_PUBLIC_KEY, FMP_API_KEY,
 #   POSTGRES_PASSWORD, FIRECRAWL_POSTGRES_PASSWORD, SEARXNG_SECRET_KEY, REGISTRY_PASSWORD
 # Required GitHub VARIABLES:
-#   OCI_REGION, DOMAIN, CHAT_SUBDOMAIN, API_SUBDOMAIN, ACME_EMAIL, ACCESS_EMAILS (JSON list),
+#   OCI_REGION, DOMAIN, APP_SUBDOMAIN (default muffin), CHAT_SUBDOMAIN (default muffin-chat),
+#   API_SUBDOMAIN, ACME_EMAIL, ACCESS_EMAILS (JSON list),
 #   LLM_PROVIDER, ORCHESTRATOR_MODELS, COLLECTOR_MODELS, REASONER_MODELS, SUMMARISER_MODEL,
 #   LANGSMITH_PROJECT, REGISTRY_USERNAME
 #
@@ -41,12 +42,14 @@ jobs:
         run: |
           cat > stack/config.yml <<EOF
           domain: "${{ vars.DOMAIN }}"
-          chat_subdomain: "${{ vars.CHAT_SUBDOMAIN }}"
+          app_subdomain: "${{ vars.APP_SUBDOMAIN || 'muffin' }}"
+          chat_subdomain: "${{ vars.CHAT_SUBDOMAIN || 'muffin-chat' }}"
           api_subdomain: "${{ vars.API_SUBDOMAIN }}"
           acme_email: "${{ vars.ACME_EMAIL }}"
           image_agent: "ghcr.io/${{ github.repository_owner }}/muffin-agent:latest"
           image_openbb: "ghcr.io/${{ github.repository_owner }}/openbb-mcp-docker:latest"
           image_ui: "ghcr.io/${{ github.repository_owner }}/agent-chat-ui-docker:latest"
+          image_app: "ghcr.io/${{ github.repository_owner }}/muffin-ui:latest"
           image_nuq_postgres: "ghcr.io/${{ github.repository_owner }}/nuq-postgres-docker:latest"
           registry_server: "ghcr.io"
           registry_username: "${{ vars.REGISTRY_USERNAME }}"
@@ -97,6 +100,7 @@ jobs:
           TF_VAR_cloudflare_zone_id:         ${{ secrets.CLOUDFLARE_ZONE_ID }}
           TF_VAR_cloudflare_account_id:      ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
           TF_VAR_cloudflare_domain:          ${{ vars.DOMAIN }}
-          TF_VAR_cloudflare_chat_subdomain:  ${{ vars.CHAT_SUBDOMAIN }}
+          TF_VAR_cloudflare_app_subdomain:   ${{ vars.APP_SUBDOMAIN || 'muffin' }}
+          TF_VAR_cloudflare_chat_subdomain:  ${{ vars.CHAT_SUBDOMAIN || 'muffin-chat' }}
           TF_VAR_cloudflare_api_subdomain:   ${{ vars.API_SUBDOMAIN }}
           TF_VAR_cloudflare_access_emails:   ${{ vars.ACCESS_EMAILS }}
diff --git a/stack/config.example.yml b/stack/config.example.yml
index 51dd606..eb0bfbd 100644
--- a/stack/config.example.yml
+++ b/stack/config.example.yml
@@ -6,8 +6,9 @@
 # API keys / passwords / tokens live in secrets.yaml.
 
 # === Public DNS / TLS ===
-domain: "example.com"           # served at https://<chat_subdomain>.<domain> and https://<api_subdomain>.<domain>
-chat_subdomain: "chat"
+domain: "example.com"           # served at https://<app_subdomain>.<domain>, https://<chat_subdomain>.<domain> and https://<api_subdomain>.<domain>
+app_subdomain: "muffin"         # new Expo/React Native web client
+chat_subdomain: "muffin-chat"   # legacy agent-chat-ui (kept alongside the new app)
 api_subdomain: "api"
 acme_email: "you@example.com"   # Let's Encrypt account email (cert-expiry notices)
 
@@ -15,6 +16,7 @@ acme_email: "you@example.com"   # Let's Encrypt account email (cert-expiry notic
 image_agent: "ghcr.io/gururafiki/muffin-agent:latest"
 image_openbb: "ghcr.io/gururafiki/openbb-mcp-docker:latest"
 image_ui: "ghcr.io/gururafiki/agent-chat-ui-docker:latest"
+image_app: "ghcr.io/gururafiki/muffin-ui:latest"
 image_nuq_postgres: "ghcr.io/gururafiki/nuq-postgres-docker:latest"
 
 # === Container registry login (so the node can pull the private GHCR packages) ===
diff --git a/stack/docker-compose.yaml b/stack/docker-compose.yaml
index 1225fdf..0e22c48 100644
--- a/stack/docker-compose.yaml
+++ b/stack/docker-compose.yaml
@@ -132,9 +132,11 @@ services:
         - "traefik.http.routers.api.entrypoints=websecure"
         - "traefik.http.routers.api.tls.certresolver=cloudflare"
         - "traefik.http.services.api.loadbalancer.server.port=8000"
-        # CORS so the browser chat UI (different subdomain) can call the API
+        # CORS so the browser UIs (different subdomains) can call the API.
+        # Web traffic is normally same-origin (each UI proxies /api), but native
+        # app builds call the API directly, so allow both UI origins.
         - "traefik.http.routers.api.middlewares=api-cors"
-        - "traefik.http.middlewares.api-cors.headers.accessControlAllowOriginList=https://{{ chat_subdomain | default('chat') }}.{{ domain }}"
+        - "traefik.http.middlewares.api-cors.headers.accessControlAllowOriginList=https://{{ chat_subdomain | default('muffin-chat') }}.{{ domain }},https://{{ app_subdomain | default('muffin') }}.{{ domain }}"
         - "traefik.http.middlewares.api-cors.headers.accessControlAllowMethods=GET,POST,PUT,PATCH,DELETE,OPTIONS"
         - "traefik.http.middlewares.api-cors.headers.accessControlAllowHeaders=*"
         - "traefik.http.middlewares.api-cors.headers.accessControlAllowCredentials=true"
@@ -159,11 +161,33 @@ services:
       labels:
         - "traefik.enable=true"
         - "traefik.swarm.network=muffin-net"
-        - "traefik.http.routers.chat.rule=Host(`{{ chat_subdomain | default('chat') }}.{{ domain }}`)"
+        - "traefik.http.routers.chat.rule=Host(`{{ chat_subdomain | default('muffin-chat') }}.{{ domain }}`)"
         - "traefik.http.routers.chat.entrypoints=websecure"
         - "traefik.http.routers.chat.tls.certresolver=cloudflare"
         - "traefik.http.services.chat.loadbalancer.server.port=3000"
 
+  # --- Muffin app (EXPOSED via Traefik: muffin.<domain>) ------------------
+  # New Expo/React Native web client. Static SPA served by nginx, which also
+  # reverse-proxies /api → langgraph-api (same-origin, like the chat UI).
+  muffin-ui:
+    image: {{ image_app }}
+    networks:
+      - muffin-net
+    deploy:
+      replicas: 1
+      restart_policy:
+        condition: any
+      resources:
+        limits:
+          memory: 256M
+      labels:
+        - "traefik.enable=true"
+        - "traefik.swarm.network=muffin-net"
+        - "traefik.http.routers.app.rule=Host(`{{ app_subdomain | default('muffin') }}.{{ domain }}`)"
+        - "traefik.http.routers.app.entrypoints=websecure"
+        - "traefik.http.routers.app.tls.certresolver=cloudflare"
+        - "traefik.http.services.app.loadbalancer.server.port=80"
+
   # --- LangGraph state + streaming (internal) -----------------------------
   langgraph-postgres:
     image: postgres:16
diff --git a/terraform/cloudflare.tf b/terraform/cloudflare.tf
index bde6969..465bdac 100644
--- a/terraform/cloudflare.tf
+++ b/terraform/cloudflare.tf
@@ -1,8 +1,9 @@
 # Cloudflare resources for the Muffin deployment (provider v5).
 #
 # Gated on var.cloudflare_domain — leave it "" and nothing here is created (OCI-only deploy).
-# When set, this creates: proxied DNS A records for <chat>/<api>.<domain> → the VM, a Zero-Trust
-# Access application per hostname, an allow-by-email policy, and a service token for API clients.
+# When set, this creates: proxied DNS A records for <app>/<chat>/<api>.<domain> → the VM, a
+# Zero-Trust Access application per hostname, an allow-by-email policy, and a service token for
+# API clients.
 #
 # NOTE: Cloudflare's SSL/TLS mode must be "Full (strict)" for the proxied origin (Traefik serves a
 # real Let's Encrypt cert via DNS-01). Set it once in the dashboard (SSL/TLS → Overview) — it is
@@ -11,6 +12,7 @@
 locals {
   cf_enabled = var.cloudflare_domain != ""
   cf_hostnames = local.cf_enabled ? {
+    app  = "${var.cloudflare_app_subdomain}.${var.cloudflare_domain}"
     chat = "${var.cloudflare_chat_subdomain}.${var.cloudflare_domain}"
     api  = "${var.cloudflare_api_subdomain}.${var.cloudflare_domain}"
   } : {}
diff --git a/terraform/muffin.tfvars.example b/terraform/muffin.tfvars.example
index f2d10ed..8e17a75 100644
--- a/terraform/muffin.tfvars.example
+++ b/terraform/muffin.tfvars.example
@@ -36,10 +36,11 @@ public_udp_ports = []
 
 # === Cloudflare (terraform/cloudflare.tf) ===
 # Leave cloudflare_domain = "" to skip all Cloudflare resources (OCI-only deploy).
-# When set, Terraform creates proxied DNS A records <chat>/<api>.<domain> → the VM,
+# When set, Terraform creates proxied DNS A records <app>/<chat>/<api>.<domain> → the VM,
 # plus Zero-Trust Access apps/policies + a service token.
 cloudflare_domain         = "example.com"
-cloudflare_chat_subdomain = "chat"
+cloudflare_app_subdomain  = "muffin"
+cloudflare_chat_subdomain = "muffin-chat"
 cloudflare_api_subdomain  = "api"
 cloudflare_access_emails  = ["you@example.com"] # allowed to pass Cloudflare Access
 # Zone ID + Account ID are on the Cloudflare zone Overview page (right sidebar → API).
diff --git a/terraform/variables.tf b/terraform/variables.tf
index 51b2b83..875fb25 100644
--- a/terraform/variables.tf
+++ b/terraform/variables.tf
@@ -104,10 +104,16 @@ variable "cloudflare_account_id" {
   default     = ""
 }
 
+variable "cloudflare_app_subdomain" {
+  description = "Subdomain for the Muffin app (Expo/React Native web client)."
+  type        = string
+  default     = "muffin"
+}
+
 variable "cloudflare_chat_subdomain" {
-  description = "Subdomain for the chat UI."
+  description = "Subdomain for the legacy agent-chat-ui."
   type        = string
-  default     = "chat"
+  default     = "muffin-chat"
 }
 
 variable "cloudflare_api_subdomain" {
